需要一个stage环境
- stage环境的数据可以定时从backup中恢复, 不包括生产的用户数据

需要一个sandbox环境
- 提供给partners, 第三方, 用于integration testing

需要一个dev环境
- 开发测试

需要一个release环境
- QA用于上线前的测试

需要一个跟当前生产环境完全一致的环境
- 用于复现生产bug
- 可以直接用当前的生产环境, 不过需要提供功能把请求路由到 待测试的机器上去, 比如, 公司内部用户的请求都被路由到了指定的机器上去

